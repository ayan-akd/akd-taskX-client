import { Helmet } from "react-helmet";
import Wip from "../Wip";

const Contacts = () => {
  return (
    <div>
      <Helmet>
        <title>TaskX | Contact Us</title>
      </Helmet>
      <Wip></Wip>
    </div>
  );
};

export default Contacts;
